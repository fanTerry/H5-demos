var merge = require('webpack-merge')
var prodEnv = require('./prod.env')

module.exports = merge(prodEnv, {
  NODE_ENV: '"development"',
  EVN_CONFIG: '"dev"',
  API_DOMAIN:'http://daily-api.esportzoo.cn',
  RES_DOMAIN:'https://daily-rs.esportzoo.com',
})
