const state = {
    matchInfo: {
        matchId: 0
    }

};
const getters = {
    getMatchInfo: state => state.matchInfo
};
const mutations = {
    setMatchInfo(state, object) {
        state.matchInfo = object;
    }
};
const actions = {
    setMatchInfo({
        commit
    }, object) {
        commit('setMatchInfo', object);
    }
};

export default {
    state,
    getters,
    actions,
    mutations
};
